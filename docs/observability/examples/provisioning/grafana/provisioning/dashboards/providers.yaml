apiVersion: 1

providers:
  - name: "00-Global"
    orgId: ${GRAFANA_ORG_ID}
    folder: "00-Global"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/00-Global

  - name: "01-Edge"
    orgId: ${GRAFANA_ORG_ID}
    folder: "01-Edge"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/01-Edge

  - name: "02-Service-Catalog"
    orgId: ${GRAFANA_ORG_ID}
    folder: "02-Service-Catalog"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/02-Service-Catalog

  - name: "03-Services"
    orgId: ${GRAFANA_ORG_ID}
    folder: "03-Services"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/03-Services

  - name: "04-Kubernetes"
    orgId: ${GRAFANA_ORG_ID}
    folder: "04-Kubernetes"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/04-Kubernetes

  - name: "05-Data-Platforms"
    orgId: ${GRAFANA_ORG_ID}
    folder: "05-Data-Platforms"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/05-Data-Platforms

  - name: "06-Business"
    orgId: ${GRAFANA_ORG_ID}
    folder: "06-Business"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/06-Business
